export interface User {
  id: string;
  ci: string;
  birthDate: string;
  role: string;
  password: string;
  hasVoted: boolean;
  placeId: number;
  token: string;
  name: string;
  lastName: string;
  numberPlace: number;
  userPhoto: string;
}